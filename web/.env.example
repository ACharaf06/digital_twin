# Optional override. /api is also the default when this variable is absent.
# Vite proxies /api to http://localhost:8000 during development.
# If the engine is unavailable, the UI disables chat and shows AI unavailable.
VITE_ENGINE_URL=/api
